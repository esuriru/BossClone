#version 330 core
out vec4 frag_color;

in vec2 UV;

uniform bool u_diffuse_map_enabled;
uniform sampler2D u_diffuse_texture0;

uniform float u_tilingFactor;

void main()
{
    frag_color = texture(u_diffuse_texture0, UV * u_tilingFactor);
}